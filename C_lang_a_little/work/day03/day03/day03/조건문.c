#include <stdio.h>

void main() {

	/*int num;
	printf("정수를 입력해주세요 : ");
	scanf_s("%d", &num);

	if (num < 3) {
		printf("num is smaller then 3!");
	}

	if (num == 3) {
		printf("num is 3!");
	}

	if (num > 3) {
		printf("num is bigger then 3!");
	}*/

	/*char alpha = 'A';

	if (alpha == 'B') {
		printf("alpha is B");
	}
	else {
		printf("alpha is not B");
	}*/

	//정수를 하나 키보드에서 입력받아 변수에 저장하고 
	//해당 변수가 홀수라면 "X는 홀수 입니다." 짝수라면 "X는 짝수 입니다"
	//출력하기

	/*int num;
	printf("정수를 입력해주세요 : ");
	scanf_s("%d", &num);
	if (num % 2 == 0) {
		printf("%d는 짝수입니다.\n", num);
	}
	else {
		printf("%d는 홀수입니다.\n", num);
	}*/

	/*int score = 70;

	if (score >= 90) {
		printf("성적은 A입니다.");
	}
	else if (score >=80) {
		printf("성적은 B입니다.");
	}
	else if (score >= 70) {
		printf("성적은 C입니다.");
	}
	else if (score >= 60) {
		printf("성적은 D입니다.");
	}
	else {
		printf("성적은 F입니다.");
	}*/

	//사용자에게 동의하면 Y를 입력하고 동의하지 않으면 N을 입력받아
	//사용자 동의 여부를 받는 프로그램을 만들기
	//Y,N 이외의 것을 입력했을때 잘못입력하셨습니다. 출력하기

	/*char agree;
	printf("동의하시겠습니까? Y/N : ");
		scanf_s("%c", &agree);
		if (agree == 'Y') {
			printf("동의하셨습니다.");
		}
		else if (agree == 'N') {
			printf("동의하지 않으셨습니다.");
		}
		else {
			printf("잘못입력하셨습니다.");
		}*/




}